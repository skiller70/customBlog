import React from "react";


import { useSelector } from "react-redux";

function Register(props) {
  const isOpen = useSelector((state) => state.isRegisterOpen.register_isOpen);

  return (
    <>
      
    </>
  );
}

export default Register;
